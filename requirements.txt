# MICO360 Doc Toolkit — runtime dependencies
PySide6>=6.6.0
pypdf>=4.2.0
PyMuPDF>=1.24.0
pdf2docx>=0.5.8
python-pptx>=0.6.23
Pillow>=10.2.0
img2pdf>=0.5.1
cryptography>=3.1        # AES-256 password protection for PDFs (pypdf)
pdfplumber>=0.11.0      # PDF -> Excel table extraction
openpyxl>=3.1.0         # write .xlsx workbooks
vtracer>=0.6.11         # Image -> SVG vector tracing (SVG rendering uses PyMuPDF)

# Word → PDF engine chain
python-docx>=1.1.0      # read .docx for the built-in fallback
reportlab>=4.0.0        # render the fallback PDF
docx2pdf>=0.1.8         # use Microsoft Word via COM when it is installed

# OCR for scanned PDFs (self-contained, no external binary).
# rapidocr pulls in CPU onnxruntime. On Windows the build swaps it for
# onnxruntime-directml so OCR runs on ANY Direct3D-12 GPU (NVIDIA/AMD/Intel)
# with automatic CPU fallback — see build/build.ps1. macOS/Linux stay on CPU
# onnxruntime (DirectML is Windows-only); the app detects this at runtime.
rapidocr-onnxruntime>=1.2.3
# onnxruntime-directml>=1.17  ; installed by the build on Windows (not here, to
#                               avoid a double-install conflict with onnxruntime)

# Build-time only (see build/):
#   pyinstaller>=6.5.0
